# Docker

## 用法

```bash
# 删除 docker 镜像和容器
python3 scripts/docker.py --command clear

# 编译 docker 镜像
python3 scripts/docker.py --command build

# 重新编译 docker 镜像
python3 scripts/docker.py --command rebuild

# 运行 docker 容器
python3 scripts/docker.py --command run
```
